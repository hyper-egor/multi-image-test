name: Test branch in docker
on: [ push ]
jobs:
  test-in-docker:
    runs-on: ubuntu-latest
    steps:
      - name: Check ou config 
        uses: actions/checkout@master
        with:
              repository:  hyper-egor/example-configs 
              path: configs
              
      - name: echo checkout 
        run: ls&&pwd
      - name: Check out repository code
        uses: actions/checkout@master
        with:
             repository: hyper-egor/conf-server
             path: conf-server
      - name: echo checkout 
        run: ls&&pwd
      - name: build conf-server
        run: gradle build
      - name: run conf-server container
        run: docker gradle dockerRun 
      #- name: run solace container
        #run: 
